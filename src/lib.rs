/// Adds two numbers
///
/// # Examples
///
/// ```
/// use conventional_commit_example::add;
///
/// let result = add(2, 3);
/// assert_eq!(result, 5);
/// ```
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}
